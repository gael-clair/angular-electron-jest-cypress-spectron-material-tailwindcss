import { Routes } from '@angular/router';
import { PageNotFoundComponent } from './components';

/**
 * PageNotFound module routes.
 */
export const routes: Routes = [
  {
    path: '',
    component: PageNotFoundComponent,
  },
];
